# datascience-ecosystem
Jupyter Notebook for Data Science Tools and Ecosystem – includes Markdown notes and Python code examples.

## React Example

The repository now includes a small React component `EnhancedApp.jsx` located in `react-app/`. It demonstrates a simplified growth tracking UI with a measurement table that supports adding and deleting entries.

